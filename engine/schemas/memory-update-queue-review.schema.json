{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "Memory Update Queue Review",
  "type": "object",
  "required": ["artifact", "memory_update_queue_review", "facts", "assumptions", "hypotheses", "missing_evidence", "confidence", "recommended_action", "guardrails"],
  "properties": {
    "artifact": {"const": "Memory Update Queue Review"},
    "memory_update_queue_review": {
      "type": "object",
      "required": ["db_path", "status", "updates"],
      "properties": {
        "db_path": {"type": "string"},
        "status": {"type": "string"},
        "updates": {"type": "array"}
      }
    }
  }
}
