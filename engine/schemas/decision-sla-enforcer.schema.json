{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "Decision SLA Enforcer",
  "type": "object",
  "required": ["recommended_decision_sla", "breach_risk", "sla_drivers", "required_checkpoint"],
  "properties": {
    "recommended_decision_sla": {"type": "string"},
    "breach_risk": {"type": "string"},
    "sla_drivers": {"type": "array", "items": {"type": "string"}},
    "required_checkpoint": {"type": "string"},
    "missed_sla_consequence": {"type": "string"}
  }
}
